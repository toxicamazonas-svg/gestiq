# Pines con tope de versión mayor: evita que una release se rompa sola
# porque Actions instaló una dependencia nueva incompatible.
playwright>=1.40,<2
openpyxl>=3.1,<4
pywebview>=5.0,<7
pillow>=10.0,<12
keyring>=24.0,<26
certifi>=2024.0.0
# customtkinter eliminado: solo lo usaba la UI vieja (gestiq.py, obsoleta)
